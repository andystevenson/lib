---
title: "andystevenson/lib"
layout: "base.njk"
---


{% set hero %}
  {% h2 'Welcome to the whacky world of testing...' %}

  {% set paragraph %}
    {%- p 'hello world &amp; all my friends' -%}
  {% endset %}

  {% div paragraph %} 
  {% ncomment 'generates <div><p>hello world</p><div>' %}

  {% set alt = 'my nice & lovely image' | escape %}

  {% img href='/', alt=alt %}
  {{ paragraph | section }}
  {{ paragraph | a(href='/')  }}
{% endset %}

{% section hero, class='hero', 'data-theme'='dark' %}
{# call a lodash function to filter the result #}
{{ p("a new paragraph") | a(href='#', 'aria-hidden'='true') | section }}

{{ [{title: 'fred'}, {title: 'joe'}] | find(['title', 'joe']) | inspect }}
{{ 'hello' | toUpper | inspect }}
{{ paragraph | toUpper | safe }}

{{ img(src='/foo') }}
{{ p('a new paragraph' + img(src='/bar')) | a }}

{# utilities ... not html #}
{# {%- inspect {a: "string", b: ["flat"] } -%} #}

{# {% button 'submit', href="foo" | a %} #}